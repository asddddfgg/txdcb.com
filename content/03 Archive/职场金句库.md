


# 名言哲思  
I hear and I forget. I see and I remember. I do and I understand — Confucius  
我听而忘，我见而记，我行而明。—— 孔子 #名言 #学习方法 #哲学  

家庭既是力量的源泉，也是痛苦的源泉。 #家庭关系 #人性洞察 
他们的人民手无寸铁。 #社会观察 #权力结构 
他看上去就像一个没有灵魂的容器。 #人性描写 #文学意象  
开法拉利，没必要加速。 #生活哲学 #阶层观察  
面对别人的求助，中国人会感到自己得到了信任。 #文化心理 #人际关系  
入关后自有大儒为我辩经。 #历史隐喻 #权力叙事  
你的思想欺骗了你 ——它让你相信你的恐惧比实际上更大。 #心理洞察 #自我成长  

# 竞争哲学  
强者自强，从来不是靠对方衰落而强，是自身实力上去了那才是强。 #个人成长 #竞争哲学  
人肉牛马想让AI当牛马。老板却不这么想，他想的是，都是牛马，为什么不选便宜的那个？ #职场 #人工智能  
客服就是背锅的，机器背锅比人要好。 #职场真相 #服务业  

# 文化观察  
未能平衡野心与自律的人 #个人成长 #成功学  
你只看到你兄弟眼中有刺，却看不到自己眼中有梁木？ #人性弱点 #自我认知  
很多人学不好外语的主要原因是：无法坚持... #学习方法 #心理学  
缺乏前进的不懈勇气。 #个人成长 #心态调整  

# 学习方法  
不只是英文才這樣，所有語言都是要一整句的聽吧~ #语言学习 #方法论  
赢得战争的最好方法就是从一开始就不要发动战争。 #战略思维 #冲突解决  

# 社会规则  
这个世界的丛林法则就是“利益”... #商业本质 #社会规则  
你们投票选出的“决策者”或者总统，都是几个资本权贵们挑选出来的“白手套”代表。 #政治现实 #权力结构  
您的个人品牌就是最终的竞争优势。 #个人发展 #职场策略  
停留在“低生态位” #社会分层 #个人发展  

# 教育反思  
统一思想 >>> 它何來創新 ??? #教育批判 #创新障碍  
一放就亂、一亂就管、一管就死、一死就放... #管理困境 #制度缺陷  
创新是玩出来的！ #教育理念 #创新思维  

# 人际关系  
交际中的常态是 无交换，不深交。 #人际关系 #社交规则  
有人不喜欢你，你也要立马不喜欢他，这是做人的基本礼貌。 #人际关系 #心理边界  

# 投资理财  
钱不是賺來的，是幫助別人解決問題之後回饋給你的禮物。 #财富哲学 #商业本质  
如果你反复纠结，太较真，除了让你自己内耗，对事情本身没有任何积极作用。 #心理调节 #效率提升  

# 权力真相  
政治上没有喜欢不喜欢，根本上都是各种利益交换 #政治本质 #权力游戏  
愚民是極權專制統治的根基  #社会控制


```dataviewjs
// 🔧 在这里修改配置 🔧
const CONFIG = {
  TARGET_TAG: "#职场",      // 要查询的标签
  LIBRARY_NAME: "职场金句"  // 库名称
};
// 🔧 配置结束 🔧

dv.header(3, `📚 ${CONFIG.LIBRARY_NAME}库`)

// 🎯 完整查询，不限制数量
const pages = dv.pages(CONFIG.TARGET_TAG)
dv.paragraph(`找到 ${pages.length} 篇包含${CONFIG.TARGET_TAG}标签的笔记`)

if (pages.length === 0) {
  dv.paragraph(`> 暂无${CONFIG.LIBRARY_NAME}`)
} else {
  let allSentences = [];
  
  for (let page of pages) {
    try {
      const file = app.vault.getMarkdownFiles().find(f => f.path === page.file.path);
      if (file) {
        const content = await app.vault.read(file);
        
        // 完整扫描，不跳过任何行
        let inCodeBlock = false;
        const lines = content.split('\n')
          .map(line => line.trim())
          .filter(line => {
            // 只排除代码块内的内容
            if (line.startsWith('```')) {
              inCodeBlock = !inCodeBlock;
              return false;
            }
            if (inCodeBlock) return false;
            
            // 确保包含目标标签且是有效内容
            return line && 
                   line.includes(CONFIG.TARGET_TAG) && 
                   line.length > 10;
          });
        
        allSentences = allSentences.concat(lines);
      }
    } catch (e) {
      console.log(`读取文件 ${page.file.name} 时出错:`, e);
    }
  }
  
  // 完整去重显示
  const uniqueSentences = [...new Set(allSentences)];
  
  if (uniqueSentences.length === 0) {
    dv.paragraph(`> 未找到具体的${CONFIG.LIBRARY_NAME}内容`)
  } else {
    // 显示所有找到的内容，不限制数量
    for (let sentence of uniqueSentences) {
      dv.paragraph(`> ${sentence}`);
    }
    
    // 完整统计
    dv.paragraph(`**完整扫描完成 · 共 ${uniqueSentences.length} 条${CONFIG.LIBRARY_NAME}**`);
  }
}
```



```dataviewjs
// 🔧 在这里修改配置 🔧
const CONFIG = {
  TARGET_TAG: "#职场",      // 要查询的标签
  LIBRARY_NAME: "职场金句"  // 库名称
};
// 🔧 配置结束 🔧

// 🆕 添加刷新按钮（放在最前面）
this.container.createEl('button', {text: '🔄 刷新数据'}).onclick = () => location.reload();

dv.header(3, `📚 ${CONFIG.LIBRARY_NAME}库`)

// 🎯 完整查询，不限制数量
const pages = dv.pages(CONFIG.TARGET_TAG)
dv.paragraph(`找到 ${pages.length} 篇包含${CONFIG.TARGET_TAG}标签的笔记`)

if (pages.length === 0) {
  dv.paragraph(`> 暂无${CONFIG.LIBRARY_NAME}`)
} else {
  let allSentences = [];
  
  for (let page of pages) {
    try {
      const file = app.vault.getMarkdownFiles().find(f => f.path === page.file.path);
      if (file) {
        const content = await app.vault.read(file);
        
        // 完整扫描，不跳过任何行
        let inCodeBlock = false;
        const lines = content.split('\n')
          .map(line => line.trim())
          .filter(line => {
            // 只排除代码块内的内容
            if (line.startsWith('```')) {
              inCodeBlock = !inCodeBlock;
              return false;
            }
            if (inCodeBlock) return false;
            
            // 确保包含目标标签且是有效内容
            return line && 
                   line.includes(CONFIG.TARGET_TAG) && 
                   line.length > 10;
          });
        
        allSentences = allSentences.concat(lines);
      }
    } catch (e) {
      console.log(`读取文件 ${page.file.name} 时出错:`, e);
    }
  }
  
  // 完整去重显示
  const uniqueSentences = [...new Set(allSentences)];
  
  if (uniqueSentences.length === 0) {
    dv.paragraph(`> 未找到具体的${CONFIG.LIBRARY_NAME}内容`)
  } else {
    // 显示所有找到的内容，不限制数量
    for (let sentence of uniqueSentences) {
      dv.paragraph(`> ${sentence}`);
    }
    
    // 完整统计
    dv.paragraph(`**完整扫描完成 · 共 ${uniqueSentences.length} 条${CONFIG.LIBRARY_NAME}**`);
  }
}


```





